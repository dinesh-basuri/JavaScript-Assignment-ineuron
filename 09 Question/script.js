// Boolean value is either true or false.
    // - Write three JavaScript statement example which provide truthy value.
    // - Write three JavaScript statement example which provide falsy value.

// there are 5 falsy values in the javascript - 0,null , undefined , NaN , ' ' .

/*
console.log(Boolean(0));
console.log(Boolean(''));
console.log(Boolean('helo'));
console.log(Boolean(undefined));
console.log(Boolean(null));
*/

/*
let height; // since the height is not defined, so it will treated as false statement hence the execution will move to else block

if(height) {
  console.log('height is defined');
}
else {
  console.log('height is not defined');
}
*/

/*
let height = 100; // since the height is defined, so it will treated as true statement hence the execution will move to if block to check the condition

if(height) {
  console.log('height is defined');
}
else {
  console.log('height is not defined');
}
*/

