// Declare an array containing the multiple values and use lastIndexOf to determine the position of the first and last occurrence of a word "pw skills".

let arr = ['hello', 'world' , 'javascript' , 'pw skills' , 'java' , 'pw skills'];

let result = arr.lastIndexOf('pw skills');
let result1 = arr.indexOf('pw skills');

console.log(result);
console.log(result1);