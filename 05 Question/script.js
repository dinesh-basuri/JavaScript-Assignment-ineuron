// Declare a varibale and assign string value to it and then split it into an array using split() method

let language = 'english is a language';

let splitWord = language.split(' ');

console.log(splitWord);